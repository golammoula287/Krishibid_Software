# ---- secrets (never commit) ----
.env
.env.*
!.env.example
*.pem
*.key

# ---- dependencies ----
node_modules/
.pnp
.pnp.js

# ---- build output ----
dist/
build/
out/
*.tsbuildinfo
.turbo/
dev-dist/

# ---- test / coverage ----
coverage/
.nyc_output/
playwright-report/
test-results/
blob-report/

# ---- logs ----
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# ---- editor / OS ----
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ---- python (ml/ is training-only, not deployed) ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
ml/data/

# ---- ML artifacts: commit the exported model, ignore raw checkpoints ----
ml/artifacts/*.pt
ml/artifacts/*.pth
ml/artifacts/*.ckpt
!ml/artifacts/*.onnx
!ml/artifacts/labels.json
!ml/artifacts/metrics.json

# ---- local scratch ----
tmp/
temp/
*.local

# ---- course/proposal material lives in the parent folder; never vendor it here ----
*.pptx
*.heic
*.docx

.vercel

# Local working plan — scratch thinking, not part of the published repo.
plan.md

# Source photography for the landing page.
# The optimised WebP in client/public/crops/ is what ships (1.5 MB -> 446 KB); these originals
# stay on disk for re-encoding at other sizes but are not worth carrying in the repo forever.
client/public/*.jpg
client/public/*.jpeg
client/public/*.jfif
client/public/*.avif
client/public/*.png
